SET TERMOUT ON
PROMPT Building demonstration tables.  Please wait.
SET TERMOUT OFF

DROP TABLE HOTEL;
DROP TABLE ROOM;
DROP TABLE BOOMNUMBER;
DROP TABLE BOOKING;
DROP TABLE CUSTOMER;
DROP TABLE ROOMTYPE;
DROP TABLE GUEST;

CREATE TABLE HOTEL
	(NORA NUMBER(3) NOT NULL,
	NORC NUMBER(3));

CREATE TABLE ROOM
    (ROOMTYPE VARCHAR(20) PRIMARY KEY NOT NULL,
    BASICPRICE NUMBER(4),
    ROOMCAP NUMBER(2));

CREATE TABLE ROOMTYPE
    (ROOMNUM NUMBER(2),
    ROOMTYPE VARCHAR(10) REFERENCES ROOM(ROOMTYPE));

CREATE TABLE ROOMNUMBER
    (ROOMNUM NUMBER(2) PRIMARY KEY ,
    ROOMAVAIL NUMBER(1));

CREATE TABLE BOOKING
    (BID VARCHAR(10) PRIMARY KEY NOT NULL,
    CID VARCHAR(10) NOT NULL,
    ROOMNUM NUMBER(2) REFERENCES ROOMNUMBER(ROOMNUM) NOT NUll,
    CDATE DATE(),
    NOG NUMBER(1));

CREATE TABLE CUSTOMER
    (CID PRIMARY KEY VARCHAR(10) NOT NULL,
    BID REFERENCES BOOKING(BID) VARCHAR(10) NOT NULL);

CREATE TABLE GUEST
    (GID PRIMARY KEY VARCHAR(10) NOT NULL,
    FNAME VARCHAR(),
    LNAME VARCHAR(),
    AGROUP NUMBER(1),
    BID REFERENCES BOOKING(BID) VARCHAR(10) NOT NULL);

INSERT INTO HOTEL (NORA, NORC) VALUES (100, 0);
    
COMMIT;

SET TERMOUT ON
PROMPT Demonstration table build is complete.
