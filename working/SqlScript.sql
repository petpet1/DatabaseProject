SET TERMOUT ON
PROMPT Building demonstration tables.  Please wait.



DROP TABLE HOTEL;
DROP TABLE ROOM;
DROP TABLE BOOMNUMBER;
DROP TABLE BOOKING;
DROP TABLE CUSTOMER;
DROP TABLE ROOMTYPE;
DROP TABLE GUEST;

CREATE TABLE HOTEL
	(NORA NUMBER(3) NOT NULL,
	NORC NUMBER(3));

CREATE TABLE ROOMTYPE
    (ROOMNUM NUMBER(2),
    ROOMTYPE VARCHAR(10) UNIQUE);

CREATE TABLE ROOM
    (ROOMTYPE VARCHAR(10),
    BASICPRICE NUMBER(4),
    ROOMCAP NUMBER(2));

CREATE TABLE ROOMNUMBER
    (ROOMNUM NUMBER(2) PRIMARY KEY ,
    ROOMAVAIL NUMBER(1)); 


CREATE TABLE BOOKING
    (BID VARCHAR(10) PRIMARY KEY,
    CID VARCHAR(10) NOT NULL,
    ROOMNUM NUMBER(2) REFERENCES ROOMNUMBER(ROOMNUM) NOT NUll,
    CDATE DATE,
    NOG NUMBER(1));

CREATE TABLE CUSTOMER
    (CID VARCHAR(10) PRIMARY KEY,
    BID VARCHAR(10) REFERENCES BOOKING(BID) NOT NULL);

CREATE TABLE GUEST
    (GID VARCHAR(10) PRIMARY KEY,
    FNAME VARCHAR(20),
    LNAME VARCHAR(20),
    AGROUP NUMBER(1),
    BID VARCHAR(10) REFERENCES BOOKING(BID) NOT NULL);

INSERT INTO HOTEL (NORA, NORC) VALUES (100, 0);

INSERT INTO ROOM (ROOMTYPE,BASICPRICE ,ROOMCAP ) VALUES('superior',170 , 3);


COMMIT;

SET TERMOUT ON
PROMPT Demonstration table build is complete.
